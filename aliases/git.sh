# Shorter alias for git with autocomplete support
# alias g='git'
# complete -o bashdefault -o default -o nospace -F _git g $1 2>/dev/null || complete -o default -o nospace -F _git g $1

# A few remappings of oh-my-zsh defaults
alias ga='gaa'
alias gs='gsb'
alias gl='gloga'
